# Verification status

2026-09-10 MCP milestone audit and interoperability correction: M0, M1 and M2 were read against their written exit criteria in the management plan and closed, leaving M3 as the only open alpha milestone. M0's last outstanding item was deployed-client discovery, closed by the 2026-09-08 live run. M1's eight exit conditions are all covered by existing tests; its residual narrative items concern tools that do not exist yet and move to M4-M6. M2's exit scenario is implemented by McpManagementWorkflowTests.McpOnlyManagementWorksInTwoDomainsAndReadOnlyCredentialCannotRepeatWrites, whose only blocker was the live client and browser evidence closed on 2026-09-08.

The 2026-09-08 entry below recorded that standard MCP clients cannot connect because the surface requires Mcp-Method and Mcp-Name headers, and framed it as a possible DnaX defect. That framing was wrong and is corrected here. The requirement originates in the official MCP SDK: the rejection message 'Missing required Mcp-Method header.' is compiled into ModelContextProtocol.AspNetCore.dll, and ModelContextProtocol.Core.dll, the client library, references the same header names, so an SDK-based client sends them. The 2026-09-08 failure was a hand-written client not implementing the 2026-07-28 revision, not a defect in DnaX or Monkeysphere. Nothing needs relaxing. What remained was a documentation gap, now closed: the MCP contract states the required headers, the _meta fields, that initialize is unavailable on this revision, and gives a minimal working exchange. Confirming with an off-the-shelf SDK client is still worth doing and fits M3's exit criterion asking for a demonstration through the intended client path. The separate unstructured-error finding from 2026-09-08 stands and is unaffected. Owner: Agent; next action: record image transfer, then an SDK-client demonstration; review: 2026-09-21.

2026-09-09 v0.1.0-alpha.4 published-artifact and upgrade verification: the Release workflow succeeded for tag v0.1.0-alpha.4. Both published ZIPs were downloaded and hash-matched against the published SHA256SUMS, the migration SQL compiled into Monkeysphere.Data.dll is LF in both, and each package ships 18 docs files including the updated verification.md, so the release genuinely differs from alpha.3 in content.

Upgrade of an installed deployment was then verified on both platforms by replacing only the application binaries and leaving the data root and service definition untouched. Linux: the alpha.3 systemd deployment from earlier the same day was started (readiness 200, 28 ledger rows at version 28), stopped, its /opt/monkeysphere payload replaced with the published alpha.4 linux-x64 build (dll sha256 533459db to 922dd930), and restarted. It reached active/running with NRestarts=0 and readiness 200 immediately, the journal reported no drift, and the ledger and database sha256 were byte-identical afterwards, including the first migration checksum sha256:83526a45 that MigrationTests pins. Windows: a temporary manual-start service was created against the published alpha.3 win-x64 build (dll sha256 66612862, database 581632 bytes), stopped, upgraded in place to alpha.4 (dll sha256 60d6aa3a) and restarted; it became ready immediately, passed eng/TestWebDeployment.ps1, and the database sha256 was unchanged. The temporary service and working directory were confirmed removed.

Scope limit, stated because it materially bounds the claim: alpha.3 and alpha.4 contain identical application code and the same application schema 28. These runs therefore verify the upgrade mechanism, that a newer published package starts against an existing data root without migration drift and without disturbing its data, but they do not exercise a schema migration between releases. An upgrade across a real schema change remains unverified until two releases differ in schema. Owner: Wixely / Agent; review: 2026-09-14.

2026-09-09 v0.1.0-alpha.4 release-candidate verification: eng/Build.ps1 and eng/VerifySupplyChain.ps1 -AuditVulnerabilities were re-run at VersionSuffix alpha.4. This release carries no application code change from alpha.3; the compiled behaviour is identical. What differs is the shipped documentation, which the release package includes: docs/ now records the live MCP client, interactive browser, packaged Linux, systemd system-unit and Windows Service verification completed on 2026-09-08 and 2026-09-09, and the two open MCP defects. The README no longer suggests pinning 0.1.0-alpha.2, which is superseded and unupgradable. Releasing alpha.4 also creates the second released version needed to verify upgrade of an installed deployment, which is the remaining platform gap. Owner: Wixely / Agent; next action: verify Windows and Linux upgrade from alpha.3 to alpha.4; review: 2026-09-14.

2026-09-09 Windows Service lifecycle verification: eng/VerifyWindowsService.ps1 passed against the published v0.1.0-alpha.3 win-x64 package in an elevated Windows PowerShell 5.1 session (5.1.19041.6456) on Windows 10 Enterprise 19045. The package was downloaded from the release and its SHA-256 matched the published SHA256SUMS entry (51d316f4...aadabfd), so this exercised the distributed artifact rather than a local build. The verifier created a temporary manual-start service, started it, ran the deployment smoke (readiness, antiforgery login and the authenticated surface) at http://127.0.0.1:15084, stopped it, confirmed both managed SQLite databases existed in the persistent data root, restarted it and repeated the smoke. The administrator credential was supplied through MONKEYSPHERE_ADMIN_PASSWORD_FILE. Cleanup was confirmed afterwards: no MonkeysphereVerification* service, no CurrentControlSet\Services registry remnant and no monkeysphere-service-verify-* working directory remain. This closes the Windows Service gap; with the 2026-09-09 Linux and systemd run, every deployment shape in the README now has live lifecycle evidence except upgrade across released versions. Owner: Wixely / Agent; next action: upgrade verification once a version later than alpha.3 exists; review: 2026-09-14.

2026-09-09 Linux package and systemd system-unit verification: the published v0.1.0-alpha.3 linux-x64 package was verified on a clean Ubuntu 24.04 WSL2 distribution installed for this purpose, against an independently installed ASP.NET Core runtime rather than a self-contained publish. The runtime is aspnetcore-runtime-10.0 10.0.11 from packages.microsoft.com at /usr/lib/dotnet; the host had no dotnet before it. Note the package targets 10.0.8 and ran on 10.0.11 by patch roll-forward. eng/VerifyLinuxPackage.sh passed end to end in framework-dependent mode: readiness, antiforgery token extraction, administrator login, the authenticated setup surface, creation of both managed SQLite databases, and a restart against the persistent data root.

The packaged deploy/systemd/monkeysphere.service unit was then installed as a real system unit with a dedicated monkeysphere system account, the application at /opt/monkeysphere owned by root, data at /var/lib/monkeysphere, and the administrator credential supplied through MONKEYSPHERE_ADMIN_PASSWORD_FILE. systemd-analyze verify reported no findings once the copy's executable bit was cleared, an artifact of copying through /mnt/c; the file is 100644 in git. Type=notify reached active/running, so the packaged application signals readiness correctly. Login through the password file succeeded and a wrong password on a fresh session was rejected. Both databases were created owned by the service account, and the account could not write into /opt/monkeysphere, confirming ProtectSystem=strict with root-owned application files. systemctl show confirms NoNewPrivileges, PrivateTmp, ProtectSystem=strict and ReadWritePaths are in effect and that the process runs as monkeysphere rather than root.

Failure recovery: SIGKILL of the main PID moved NRestarts from 0 to 1 under Restart=on-failure, produced a new PID, and readiness returned to 200. Boot enablement: after wsl --shutdown, a cold boot showed 5s uptime, NRestarts=0, the unit enabled and active, and readiness at 200 with no manual start, and the 581 KB application database from before the shutdown was still present. This supersedes the earlier static systemd-analyze and transient user-service checks below, and closes the packaged Linux and system-unit gaps. Upgrade across released versions remains unverified because no version later than alpha.3 exists. Owner: Wixely / Agent; next action: Windows Service lifecycle on an elevated host; review: 2026-09-14.

2026-09-08 live MCP client and interactive browser verification: the published v0.1.0-alpha.3 win-x64 package was downloaded, extracted and run as a standalone process against a fresh data root with DnaX remote access and the MCP surface enabled. This is deployed-artifact evidence over a real HTTP transport, not the in-process test host. Browser: Chromium drove first-run login, the four-level setup wizard with a 10-preset review and transactional install, the Settings and Remote access pages, permission selection, credential rotation, surface activation and the redacted audit table. The new Export contacts permission and the corrected Import contacts description both render in the live permission UI, which closes the permission-UI gate for contract 1.15. MCP: a standalone Python client performed tools/list and tools/call over the randomized endpoint, seeing 52 registered tools, contract 1.15, application version 0.1.0-alpha.3, schema 28 and registry 4, with granted scopes exactly contacts.export, instance.read, records.read and records.write. Two Person records were created through create_record and exported through export_contacts in four 64-byte ranges; the reassembled 196-byte vCard 4.0 document matched its advertised SHA-256 digest and contained the expected FN, NICKNAME and EMAIL properties. Grant separation held live: preview_contact_import reported allowed=false and returned permission_denied. Unknown record IDs, an out-of-range count and an unknown domain each returned structured validation_failed, and the audit table recorded allowed and failed calls without names or credential material.

Two defects were found and are recorded in docs/roadmap.md. First, the deployed MCP surface rejects a standard MCP client: every request must carry a DnaX-specific Mcp-Method header, and tools/call must also carry Mcp-Name, or the request fails with HTTP 400 before reaching a tool. Second, the older records.read tools (list_record_types, search_records, list_field_definitions, get_record, query_record_types) return an unstructured 'An error occurred invoking' message for an invalid domainId, where query_records and every write and export tool return a structured validation_failed. Both were found only because this ran outside the test host. Owner: Agent; next action: confirm whether the header requirement is part of the MCP revision or DnaX-specific, then decide the interoperability fix; review: 2026-09-14.

2026-09-07 v0.1.0-alpha.3 published-artifact verification: the Release workflow succeeded for tag v0.1.0-alpha.3 (commit 6287182); the container job published ghcr.io/wixely/monkeysphere:0.1.0-alpha.3 and the Windows job ran the full suite, a current NuGet advisory audit, packaging and asset publication. All three published artifacts were then downloaded and inspected rather than trusted: both release ZIPs hash to exactly the values in the published SHA256SUMS.txt, and the migration SQL compiled into Monkeysphere.Data.dll is LF in the win-x64 ZIP, the linux-x64 ZIP and the container image. All three artifacts of this tag therefore agree with each other and with current main, which is the property the alpha.1 and alpha.2 releases lacked. The alpha.1 and alpha.2 containers were separately verified LF and the ZIPs of those tags CRLF, so those two tags ship mutually unreadable artifacts and their ZIP deployments cannot upgrade; both release notes state this. Outstanding for this release: live MCPHub and interactive browser verification, Windows Service and packaged-Linux lifecycle checks, and execution of the distributed packages on clean hosts. Owner: Wixely / Agent; review: 2026-09-14.

2026-09-07 v0.1.0-alpha.3 release-candidate verification: eng/Build.ps1 passed all 274 tests (46 Core, 94 Data, 134 Web), locked restore, text/supply-chain checks and Release build with zero warnings/errors at VersionSuffix alpha.3. eng/VerifySupplyChain.ps1 -AuditVulnerabilities reported no known direct or transitive NuGet vulnerability from the configured sources; this audit is time-sensitive and is not permanent evidence. eng/PackageRelease.ps1 produced the win-x64 and linux-x64 ZIPs and a two-entry SHA-256 file. The migration line-ending regression that stranded alpha.1 and alpha.2 was verified fixed at the binary level: the migration SQL compiled into Monkeysphere.Data.dll now contains LF, checked by the same UTF-16 inspection that found CRLF in both published alpha.2 ZIPs, and this build was produced on Windows with core.autocrlf=true, the condition that caused the original defect. Data created by alpha.1 or alpha.2 packages remains unopenable by this build, by decision. Live MCPHub, interactive browser, Windows Service and packaged-Linux verification remain outstanding, and the alpha container's line endings are still unverified. Owner: Wixely / Agent; next action: push the v0.1.0-alpha.3 tag to publish, then verify the published artifacts; review: 2026-09-14.

2026-09-07 archived backup and migration ledger verification: eng/Build.ps1 passed all 274 tests (46 Core, 94 Data, 134 Web), locked restore, text/supply-chain checks and Release build with zero warnings/errors. A genuine single-domain format 1 package was produced by building the last format 1 commit (30e8bea, application schema 18) in a separate worktree, and is committed unchanged as tests/Monkeysphere.Web.Tests/Fixtures/format-1-schema-18.monkeysphere-backup. ArchivedBackupCompatibilityTests restores it and verifies the legacy single-database layout, the absence of any domain directory or manifest domain list, the retained original image, the absence of regenerable derivatives, and the archived record, alias, field value and image row. This closes the format 1 compatibility gap listed below for the restore path itself. It also found a blocking defect: starting the application against the restored data root fails with DnaX migration state 'Drifted', because 18 applied migrations' checksums differ from the current code. The cause is established rather than inferred: DnaX hashes migration text verbatim without normalizing newlines, and .gitattributes gained eol=lf rules for the migration sources only in commit 4eddd54, so a pre-4eddd54 Windows checkout recorded CRLF-derived checksums. Building current main with the migration sources converted to CRLF reproduces the fixture's stored checksums exactly (v1 246563bc, v2 ddc4b39f, v3 f61d4f27), proving line endings are the sole cause and that the migration text is unchanged. The affected checksums are therefore computable exactly, so a deterministic repair is possible without a DnaX change. The restore path and backup format are not at fault. Both published alpha ZIPs are affected, established by reading the migration SQL out of the Monkeysphere.Data.dll inside each downloaded release asset: v0.1.0-alpha.2 (commit 30e8bea) contains CRLF in both win-x64 and linux-x64, which are built by the same windows-latest job. The alpha.1 and alpha.2 containers were verified LF by fetching both published images from the GHCR blob API and reading app/Monkeysphere.Data.dll, so the container and the ZIPs of the same tag genuinely disagree: container deployments upgrade normally while ZIP deployments are stranded. The decision recorded in the roadmap is to supersede both alphas rather than ship a ledger repair. Prevention was verified: .gitattributes now applies * text=auto eol=lf repository-wide, and MigrationTests.ReleasedMigrationChecksumsRemainStable was confirmed to fail on a deliberately CRLF-converted build of current main, so eng/Build.ps1 blocks a recurrence during release. Recorded in docs/roadmap.md under migration ledger compatibility. Owner: Wixely / Agent; next action: verify the container's line endings and publish a replacement prerelease after the domains merge; review: 2026-09-14.

2026-09-07 contact export verification: eng/Build.ps1 passed all 272 tests (46 Core, 94 Data, 132 Web), locked restore, text/supply-chain checks and Release build with zero warnings/errors. Contract 1.15 registers 52 tools and adds export_contacts under a new contacts.export grant. Nine new production-protocol cases cover multi-range streaming with a stable whole-document SHA-256 digest, single-response export, digest change on record edit and on reordered selection, re-import of the exported document, empty final range at the end offset and failure beyond it, empty/duplicate/unknown/non-Person/over-100 selections, count and offset bounds, foreign-domain and unknown-domain denial, denial under contacts.import and four other grants, capability reporting of the new tool and its limits, and confirmation that contacts.export authorizes no import, read, write or delete tool. Export reuses the shared Core export service and stages nothing, so no new quota, expiry or restore-invalidation surface is introduced. The prior entry's Data count of 93 was inaccurate; 94 is observed both with and without this change. Live MCPHub and interactive browser verification remain outstanding. The format 1 fixture gap was closed later the same day; see the entry above. Owner: Agent; next action: record image transfer to complete M3; review: 2026-09-14.

2026-09-07 contact import apply verification: eng/Build.ps1 passed all 262 tests (46 Core, 93 Data, 123 Web), locked restore, text/supply-chain checks and Release build with zero warnings/errors. Contract 1.14 registers 51 tools and application schema 28 passes every historical upgrade. Data tests cover all four actions, final record IDs/revisions, opaque provenance, competing exact applies, restart/cancellation replay, changed-key and consumed-preview conflicts, stale rollback, atomic audit failure, 1,000 paged outcomes, command/outcome quotas, expiry cleanup and tombstones. Production MCP tests cover apply/result paging, invalid actions, permission denial, replay after source cancellation and credential-generation isolation. Backup/restore preserves committed receipts/outcomes and records while invalidating transient upload/preview handles. Live MCPHub and interactive browser verification remain outstanding. Owner: Agent; next action: bounded authenticated selected-contact export, then images and remaining management milestones; review: 2026-09-14.

2026-09-07 contact preview MCP verification: eng/Build.ps1 passed all 255 tests (46 Core, 87 Data, 122 Web), locked restore, text/supply-chain checks and Release build with zero warnings/errors. Contract 1.13 registers 49 tools, including preview_contact_import, get_contact_import_preview and read_contact_import_evidence. Six new production-protocol cases cover four denied grants, full 5 MiB preview, missing Person schema, paging/truncation, duplicate recommendations, complete multi-range Unicode/opaque evidence reconstruction, range/response bounds, unchanged record counts, stale advisory status and same-key replay, domain/credential isolation and cancellation. Existing restart, quotas, migration, audit rollback and backup/restore checks remain green. Live MCPHub and interactive browser verification remain outstanding. Owner: Agent; next action: revision-checked import with atomic durable outcomes/receipts, then export/images and remaining management milestones; review: 2026-09-14.

2026-09-07 durable contact preview verification: eng/Build.ps1 passed all 249 tests (46 Core, 87 Data, 116 Web), locked restore, text/supply-chain checks and Release build with zero warnings/errors. Staging migration 3 passes every historical schema upgrade. Seven Data cases cover complete mapping/opaque-property/duplicate-evidence serialization, restart replay, stale revision rejection by shared apply, credential/domain isolation, strict paging, owner/domain/global/byte/history quotas, oversized evidence, expiry/tombstones, cancellation cleanup, competing retries and audit-failure rollback. The backup/restore test now creates a real contact preview and verifies its handle is invalid after restore. No new MCP tools are exposed by this storage checkpoint; transport-bounded inspection, replayable import receipts/outcomes and live-client verification remain outstanding. Owner: Agent; next action: MCP inspection projections and the atomic import receipt transaction; review: 2026-09-14.

2026-09-07 commit verification: eng/Build.ps1 passed all 242 tests (46 Core, 80 Data, 116 Web), locked restore, text/supply-chain checks and Release build with zero warnings/errors. Application migration 27 passes historical migration verification. New contact-import tests cover stale prepared imports, alias/schema/provenance changes, restart persistence, rollback and competing applies with one winner. Duplicate discovery also handles existing contacts with no mapped values. Browser streaming and stale-preview reset are compiled; interactive browser and deployed MCPHub checks remain outstanding. Owner: Agent; next action: durable MCP contact preview/inspection and replayable atomic apply; review: 2026-09-14.

2026-09-07 MCP upload verification: eng/Build.ps1 passed all 239 tests (46 Core, 77 Data, 116 Web), locked restore, text/supply-chain checks and Release build with zero warnings/errors. Contract 1.12 exposes 46 tools. New production-tool tests cover full 5 MiB transfer, begin/chunk/completion replay, exact purpose grants, cross-domain denial, malformed Base64/digests/vCards, expiry cleanup, credential generation changes, revocation mid-transfer and 4096/9216/65536-byte request configurations. Audit inspection excludes names, credential secrets and file digests; Data tests prove audit-failure rollback for begin/write/cancel. Staging schema 2 passes historical migration verification. Earlier upload-exposure gaps are superseded. Contact preview/apply/export, images, deployed MCPHub and interactive permission UI remain unverified. Owner: Agent; next action: durable contact preview/apply; review: 2026-09-14.

2026-09-07 streaming-validation verification: eng/Build.ps1 passed all 228 tests (46 Core, 76 Data, 106 Web), locked restore, text/supply-chain checks and Release build with zero warnings/errors. New tests compare streaming and synchronous fingerprints/extension serialization, split UTF-8 at 1/7/16384-byte reads, cross-buffer surrogate pairs, exact 5 MiB acceptance, oversize/malformed input, card/property caps, cancellation and 50000 folded lines. Staged validation verifies byte ownership, integrity-versus-semantic failure, full-consumption enforcement, lease disposal, cancellation and no record import. Existing vCard import/export and full MCP workflows still pass. Upload tools, auditing, preview/apply and live-client transfer remain unverified. Owner: Agent; next action: audited authenticated upload lifecycle tools; review: 2026-09-14.

2026-09-07 upload-storage verification: eng/Build.ps1 passed all 220 tests (39 Core, 75 Data, 106 Web), locked restore, text/supply-chain checks and Release build with zero warnings/errors. The separate staging manifest passes historical migration verification. Tests cover concurrent identical chunks, transactional rollback on offset-update failure, gaps/overlaps, digest failures, restart, owner/domain rejection, cancellation, expiry, begin-key tombstones, owner/domain/global/byte/history quotas and the chunk-count cap. Backup/restore integration proves staged bytes are excluded from packages, removed from successful-restore rollback and inaccessible through old handles. Internal sealing checks integrity only. MCP upload authorization, audit, content validation, durable contact previews/apply and deployed transfer remain unverified. Owner: Agent; next action: authenticated contact upload adapters and validation; review: 2026-09-14.

2026-09-07 structured-search verification: eng/Build.ps1 passed all 211 tests (39 Core, 67 Data, 105 Web), locked restore, text/supply-chain checks and Release build with zero warnings/errors. Contract 1.11 exposes 41 tools. Real MCP tests cover combined filters, numeric/name sorting, literal wildcard and injection text, aliases, empty-page totals, exact-date boundaries, non-finite bounds, malformed nested inputs, permission denial and foreign references. The full MCP-only workflow completes setup/schema/typed records/relationships/edit/search/reviewed deletion in two domains, preserves the surviving records and denies every captured write after read-only credential rotation. Earlier structured-search and two-domain-scenario gaps below are superseded. Live MCPHub and interactive browser verification remain outstanding. Owner: Agent; next action: bounded contact upload and durable preview sessions; review: 2026-09-14.

2026-09-07 domain-creation verification: eng/Build.ps1 passed all 205 tests (39 Core, 67 Data, 99 Web), locked restore, text/supply-chain checks and a Release build with zero warnings/errors. Contract 1.10 exposes 40 tools; registry migration 4 passes historical migration verification. New tests cover hidden reservations after publication/audit failure, changed-key ownership denial, reserved names, pre-cancellation, restart with and without initialized storage, browser retry and restart, pending quota with successful owned retry, preservation of unowned storage, MCP scope denial/concurrent replay and fresh-domain setup. A real backup taken with pending intent validates, restores without the unpublished database, and completes the same domain identity at startup. Earlier creation gaps below are superseded. Live MCPHub and interactive browser failure/recovery remain unverified. Owner: Agent; next action: structured search and the complete two-domain MCP scenario; review: 2026-09-14.

Last verified: 2026-09-07 (local build and MCP integration tests; prior platform evidence retains its dates)

## Verified evidence

- The domain-revision 2026-09-07 `eng/Build.ps1` passed all 188 tests (39 Core, 60 Data, 89 Web), locked restore, text/supply-chain checks and a Release build with zero warnings/errors. Domain registry historical migration verification covers version 2. Tests prove persisted opaque domain revisions, exactly one winner among competing renames, changed tokens when restoring an earlier name, duplicate-name rollback, pre-cancelled rename rejection, fresh-provider restart persistence and matching revisions from both MCP domain-listing tools. Existing backup/restore tests pass. This is the shared rename foundation; remote domain mutations, creation recovery and interactive browser conflict handling remain unverified (owner: Agent; review: 2026-09-14).

- The onboarding 2026-09-07 `eng/Build.ps1` passed all 186 tests (39 Core, 59 Data, 88 Web), locked restore, text/supply-chain checks and a Release build with zero warnings/errors. Contract 1.8 registers 38 tools. Tests prove setup/catalog discovery is paged and read-only, implicit completion has no fabricated timestamp, revision tokens are domain-bound, selected setup replays concurrent requests, blank setup requires acknowledgement, later preset installation works, and stale catalogs/selections fail. Data tests cover restart replay, subsequent schema changes and complete rollback of the largest starter selection when audit insertion fails. Domain creation in the setup integration fixture still uses Core; MCP domain creation/rename, live MCPHub discovery and interactive browser verification remain outstanding (owner: Agent; review: 2026-09-14).

- The schema-command 2026-09-07 `eng/Build.ps1` passed all 179 tests (39 Core, 57 Data, 83 Web), locked restore, text/supply-chain checks and a Release build with zero warnings/errors. Contract 1.7 registers 31 tools, including custom record-type creation and field creation/attachment. An MCP-only scenario builds a custom schema, reuses a field, and creates a typed record with aliases. Tests cover scoped denial, required-value gaps, stale type/field revisions, foreign references, configuration validation, restart replay, competing edits and full rollback on audit failure. Shared service tests verify browser-path revision rejection and required attachment validation. Historical migration verification covers version 26. Interactive browser behavior, live deployment and MCPHub discovery remain unverified (owner: Agent; review: 2026-09-14).

- The relationship-command 2026-09-07 `eng/Build.ps1` passed all 172 tests (39 Core, 55 Data, 78 Web), locked restore, text/supply-chain checks and a Release build with zero warnings/errors. Contract 1.6 registers 28 tools including relationship-type creation and link creation/deletion. Real MCP calls test separate grants, capability accuracy, revocation, rotated receipt ownership, concurrent identical creates, duplicate symmetric links, retries after deletion, stale type/record/link revisions, mixed-domain references, normalization, bounds and metadata-only audit. Data tests prove restart replay, receipt expiry/conflicts and complete rollback of type creation, link creation and link deletion when audit insertion fails. Browser interaction and live MCPHub deployment remain unverified; this is local test-host evidence. Owner: Agent; next review: 2026-09-14.

- The relationship-foundation 2026-09-07 `eng/Build.ps1` passed all 161 tests (39 Core, 53 Data, 69 Web), locked restore, text/supply-chain verification and a Release build with zero warnings/errors. Historical migrations now cover version 25. Tests prove persistent type/link revisions, matching tokens from both endpoints, competing edits with one winner, stale rename/retirement/creation/removal rejection, note-change invalidation and invalid-directionality rejection. Actual MCP calls verify revision metadata in both paged and legacy reads. Browser handlers submit captured revisions and compile; interactive browser verification and scoped replayable MCP relationship writes remain outstanding (owner: Agent; review: 2026-09-14).

- The deletion-slice 2026-09-07 `eng/Build.ps1` passed all 159 tests (39 Core, 51 Data, 69 Web), locked restore, text/supply-chain checks and a Release build with zero warnings/errors. Contract 1.4 adds a separate deletion grant, preview/inspection, deletion and status tools. Real MCP tests cover direct empty-record deletion, mandatory review for dependent data, stale impact, concurrent retries, expiry, credential rotation, domain isolation and audit. Data tests cover impact invalidation for media/relationships/reminders/graph/import data and content edits, cascading deletion with preservation of related records/views, restart cleanup, receipt replay after preview cleanup, queue quota, Windows file locks and an image upload overlapping deletion. Historical schema verification includes migration 24; graph scale and existing image/backup/restore tests still pass. This verifies local behavior, not live deployment or Linux file-lock behavior.
- The subsequent 2026-09-07 `eng/Build.ps1` passed all 151 tests (39 Core, 46 Data, 66 Web), locked restore, text/supply-chain checks and a Release build with zero warnings/errors. MCP contract 1.3 adds persisted batch preview/inspection/apply. Tests cover duplicate concurrent apply, whole-batch rollback after a browser-service edit, receipt replay after preview cleanup and restart, stale schema, expiry, quota enforcement, owner/domain/scope isolation and cleanup across domains. Historical migration verification now includes version 23. Targeted formatting verification passed. A shutdown race exposed by the new background worker was fixed in the test harness by asynchronously waiting for the application entry point to release its instance lock before teardown/restart; full restart and offline restore tests pass. No new live deployment or platform lifecycle verification is claimed.
- An earlier 2026-09-07 `eng/Build.ps1` passed all 144 tests (39 Core, 43 Data, 62 Web), locked restore, text/supply-chain checks and a Release build with zero warnings/errors. Seventeen MCP discovery/write cases now include actual validate/create/patch invocation, explicit write grants, rotation ownership, revocation, preservation of unmentioned typed data, explicit clearing, required-field failure rollback, duplicate retries, competing edits, replay after deletion, wrong-domain rejection, unknown nested input rejection and metadata-only failure audit. Record-write limits are advertised by contract 1.2. Local tests do not establish deployed-client or live browser behavior.
- The 2026-09-07 `eng/Build.ps1` run passed locked restore, text encoding and supply-chain checks, a Release build with zero warnings/errors, and all 133 tests (38 Core, 39 Data, 56 Web). Twelve MCP discovery cases cover live test-host discovery/invocation, narrow and legacy scopes, unrelated-scope denial, credential revocation, catalog consistency, choice configuration, canonical temporal values, paged 105-type/505-relationship fixtures, domain isolation, input schemas, permission-preserving rotation and a test-only 256 KiB binary chunk probe with oversize rejection. Targeted formatting verification passed for the changed C# files. This verifies repository behavior, not deployed MCPHub transfers, browser interaction with permission checkboxes, or new platform lifecycle behavior.
- A subsequent 2026-09-07 full build passed all 139 tests (39 Core, 43 Data, 57 Web), including historical migrations through version 22, stale record/schema rejection, child-table revision changes, concurrent editor/command outcomes, batch rollback, durable restart replay, retry expiry/hash conflicts, canonical request hashing and credential-generation binding. Formatting verification passed for the changed transaction code. At that checkpoint, write tools were not yet exposed; subsequent tool-level evidence is recorded below. Live browser stale-edit interaction and deployed-client behavior remain unverified.
- The 2026-09-03 Release build completed with zero warnings and all 119 tests passed. Supply-chain verification passed for all 12 vendored files.
- `dotnet format Monkeysphere.slnx --verify-no-changes --no-restore` passed.
- Authenticated editor rendering and static-asset tests cover the private map-pin control and vendored OpenLayers delivery.
- Core, SQLite integration, and authenticated rendering tests cover map-query bounds, single and multi-field filters, pagination, R-tree migration history, approximation-radius viewport intersection, structured location summaries, private map access, and disabled-by-default persistence of the external-tile choice.
- Backup integration tests create and revalidate a package containing both online SQLite snapshots and one original image, while proving that derivatives and data-protection keys are excluded.
- An offline restore integration test changes live data after backup, restores the earlier package with rollback retention, restarts against the restored data root, and verifies database rollback plus lazy image-derivative regeneration.
- Schedule tests cover daily, weekly, and end-of-month recurrence calculations; scheduling remains off by default in the application and Docker configuration.
- Core, SQLite integration, and authenticated rendering tests cover graph limits, alias search, all/connected/isolated modes, record-type filtering, neighbour selection, cover-image and record-type-symbol selection, deterministic node truncation, graph-view filter and coordinate persistence, coordinate validation, private route protection, and vendored Cytoscape.js delivery.
- Web rendering checks cover delivery of the early theme initializer, its local-storage persistence contract, the accessible header toggle, and live graph-theme integration.
- A production-path scale regression creates 10,000 records and 50,000 relationships, completes a one-hop focused query under a 10-second cancellation deadline, and proves simultaneous 500-node and 2,000-edge truncation. This verifies the accepted boundary, not a latency SLA.
- Application tests verify the global anti-framing, origin-only cross-site referrer, MIME-sniffing, permissions, base-URI, and object-embedding response headers.
- `eng/VerifySupplyChain.ps1 -AuditVulnerabilities` reported no known direct or transitive NuGet vulnerability from the configured sources on 2026-09-03. The audit is time-sensitive and is not treated as permanent evidence.
- `eng/PackageRelease.ps1` produced separate framework-dependent Windows x64 and Linux x64 ZIPs plus a two-entry SHA-256 file. Archive inspection found no PDB, database, backup, key, secret, or log candidate; execution of the Linux package remains unverified.
- The packaged Windows x64 executable passed readiness, default-credential sign-in, authenticated setup rendering, managed-database creation, process restart, and persistent-data-root smoke checks twice on 2026-08-29.
- The systemd unit passed `systemd-analyze verify` against isolated existing command/data paths and retained its notify, privilege-escalation, private-temporary-directory, and read-only-system hardening directives. This was a static unit check, not a service lifecycle test.
- A temporary self-contained Linux x64 publish used the already-cached .NET 10.0.8 runtime pack and passed fresh startup, readiness, default-credential authentication, managed-database creation, process restart, and persistent-data-root checks on the WSL2 Linux kernel without installing or configuring the distribution.
- A non-privileged transient user service under systemd passed `Type=notify` readiness, default-credential authentication, stop/start lifecycle, and persistent-data-root checks with the same temporary Linux publish. Packaged system-unit installation and boot enablement remain separate gaps.
- The digest-pinned Dockerfile built successfully with rootless Podman. The resulting OCI container ran as UID 1654, passed readiness and default-credential authentication, created both managed databases in a named volume, and passed the same checks after container removal and recreation against that volume.
- Docker Engine 28.5.2 and Docker Compose 2.40.3 ran inside an isolated, ephemeral nested daemon and passed the exact Compose build, readiness, default-credential authentication, database-volume, stop/start, and cleanup workflow. This verifies the Compose deployment behavior without installing a host daemon; production host storage, networking, and boot policy remain operator concerns.
- DnaX historical verification constructed and upgraded every Monkeysphere schema version to the same canonical schema.
- Domain regression tests prove that legacy data remains in the renameable Default domain, additional domains receive independent migrated databases and setup state, duplicate names work across domains, cross-domain identifiers resolve as absent, invalid selectors fail closed, and graph preferences are namespaced per domain.
- Backup and offline-restore tests cover deployment-wide format 2 packages containing multiple domains and original images. The validator and offline restore continue to accept legacy format 1 packages; a dedicated archived-format fixture remains desirable before release.
- Remote-surface tests cover authenticated domain discovery, explicit HTTP header selection, optional MCP `domainId` selection, cross-domain not-found behavior, and malformed or unknown selectors returning a client error.
- Automated tests cover authentication and antiforgery, cookie transport behavior, restart persistence, alias persistence, update validation and search, structured-location validation, coordinate normalization, context-only approximations, persistence, search/filtering and remote serialization, image decoding and normalization, captions, cover selection, ordering, non-destructive correction validation, authenticated derivative and original delivery, byte-identical original retention, image deletion/promotion and record cascading, temporal precision, exact-date calendar inclusion and filtering, approximate/coarse-date exclusion, authenticated calendar rendering and export, iCalendar escaping and UTF-8 folding, reminder eligibility, duplicate prevention, persistence across date edits, dismissal and cleanup, vCard 3.0/4.0 parsing, malformed-input rejection, semantic extension round-tripping, mapping preview, exact-import, normalized phone, saved-record and within-file duplicate detection, conservative name-only handling, create/skip/merge/replace behavior, whole-batch rollback, authenticated selected-contact export, relationship lifecycle and directionality, saved-view CRUD, multiple filters, field grouping/sorting, rename/retirement stability, previewed compatible-field merging, explicit conflict resolution, stale-preview rejection, saved-view reference migration, fail-closed field conversion, record-type retirement, record/type/view migration, required-rule relaxation, preset provenance and versioning, customizable transactional starter installation, persistent blank-slate setup, wizard example rendering, remote-disabled defaults, DnaX API route and credential rotation, scope denial, credential separation, relationship and image-metadata retrieval, and an MCP tool call.
- An MCP-driven browser smoke test verified `admin` / `admin` login, record-type and field creation, saved-view creation and duplication, record creation, saved filter application, grouping, and configured table-column rendering in Chromium.
- A separate Release process smoke test applied DnaX migration 5 to a fresh SQLite data root, accepted the default administrator login, and served the authenticated four-tier wizard and its examples successfully.
- A focused Chromium accessibility review covered desktop and 390 by 844 mobile layouts, keyboard access to the skip link, setup selection state, mobile navigation wrapping, and the accessibility tree. Graph and map visualizations now expose bounded standard-control or HTML-list alternatives.
- A Chromium visual and rendering sweep covered the first-run wizard and every primary authenticated route at desktop and 390 by 844 mobile sizes. It confirmed the sans-serif stack, compact spacing, zero document overflow, correct Unicode rendering, no visible Blazor error UI, and successful delivery of the fingerprinted favicon.
- An MCPHub Playwright contrast sweep covered login plus the overview, structures, records, calendar, record editor, graph, and settings surfaces in both themes, with representative authenticated routes repeated at 390 by 844. It caught and verified the fix for a light graph canvas paired with dark-theme labels; a clean graph reload then produced only normal Blazor connection messages and successful asset, data, and image responses.
- A 2026-09-03 MCPHub Playwright map review used three fictional central-London locations. The default-off state made no OpenStreetMap resource request. After explicit enablement in Settings, one final-zoom set of 15 tile requests returned HTTP 200; the visible disclosure, OpenStreetMap attribution, distinct record-type field labels, three separate pins, and accessible location-list alternative all rendered correctly.
- An MCPHub Playwright dashboard sweep covered the default People and Birthday selection, persisted settings, a populated annual birthday projection, record-image placeholders, desktop light mode, mobile dark mode at 390 by 844, and overflow checks at 320 pixels. The populated result displayed the historical birthday as its next annual occurrence above the configured People list. Authenticated rendering tests cover both placeholder and private-thumbnail rows.
- A 2026-09-03 MCPHub browser sweep covered the shared monkey logo, blank-setup warning dialog, multi-category selection and reordering, preserved Cat/People/Dog dashboard order, About release metadata, configuration-enabled Debug controls, typed destructive confirmation, completed reset back to first-run setup, desktop light mode, and mobile dark mode at 390 by 844. The inspected pages had clean console output after navigation settled.
- An MCPHub Playwright graph sweep covered the unfiltered all-record default, debounced name and alias lookup, automatic single-result centring, comma-separated multi-selection, isolated mode, persisted record-type checkboxes, graph-view creation and reopening through Structures > Saved views, and a 390 by 844 responsive resize. It caught and verified fixes for malformed Razor `else` output, single-item local-storage serialization, interrupted slow typing, and stale Cytoscape sizing.
- A Chromium combobox sweep covered every route that previously rendered a native select or datalist. It confirmed HTML/CSS listboxes with visible type filtering, mouse and Enter selection, outside-click closing, open custom field-type identifiers, no native select remnants, zero mobile overflow, and clean console/network logs. Authenticated rendering tests assert the combobox semantics and behavior asset.
- A 2026-09-03 MCPHub Chrome review covered domain creation, immediate header-menu refresh, protected-cookie switching, independent first-run setup, return to a completed Default domain, and the Domains settings layout in desktop light and dark themes. The final reload had no console errors or failed network requests.
- A 2026-09-03 MCPHub Chrome graph review saved a three-node layout, added a newly matching record, and reopened the view. The original records retained their relative positions, the new record occupied a separate collision-free position, saving the expanded layout succeeded, and the final page had no console errors or failed network requests.

## Not yet verified

- Booting the application against a restored pre-4eddd54 data root. Restore of the preserved, independently produced format 1 fixture now passes and its data is intact; startup against it fails with DnaX migration drift, recorded in the newest entry above and in the roadmap.
- Dedicated screen-reader, Windows high-contrast, browser-zoom, and automated accessibility-scanner verification.
- Upgrade across a release that changes the application schema. The alpha.3 to alpha.4 upgrade was verified on both Windows and Linux on 2026-09-09, but those releases share schema 28, so no migration ran. Repeat when two releases differ in schema.

These are verification gaps, not support claims. Re-run the full suite and update this file when an appropriate environment is available.


2026-09-07 registry-command verification: eng/Build.ps1 passed all 195 tests (39 Core, 62 Data, 94 Web), locked restore, text/supply-chain checks and Release build with zero warnings/errors. Contract 1.9 exposes 39 tools; registry migration 3 passes historical migration and existing backup/restore tests. Rename tests cover scope denial, concurrent replay, restart persistence, later browser edits, credential rotation, conflicting targets/payloads, expiry, quotas and atomic rollback on audit failure. This supersedes earlier remote-rename gaps below. Recoverable domain creation, live MCPHub and interactive browser verification remain outstanding. Owner: Agent; next action: recoverable creation; review: 2026-09-14.
