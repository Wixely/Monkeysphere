<nav class="section-tabs" aria-label="Structure sections">
    <a class="@(Active == "record-types" ? "active" : null)"
       aria-current="@(Active == "record-types" ? "page" : null)"
       href="/structures">Record types</a>
    <a class="@(Active == "relationship-types" ? "active" : null)"
       aria-current="@(Active == "relationship-types" ? "page" : null)"
       href="/structures/relationship-types">Relationship types</a>
    <a class="@(Active == "saved-views" ? "active" : null)"
       aria-current="@(Active == "saved-views" ? "page" : null)"
       href="/structures/saved-views">Saved views</a>
</nav>

@code {
    [Parameter, EditorRequired]
    public string Active { get; set; } = string.Empty;
}
